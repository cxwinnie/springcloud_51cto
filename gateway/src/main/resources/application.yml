server:
  port: 8088

spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8761/eureka,localhost:8762/eureka,localhost:8763/eureka
#        server-addr: localhost:8841,localhost:8843,localhost:8845

